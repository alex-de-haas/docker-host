# Scripts

This directory is reserved for repository-level bootstrap scripts.

The planned Unix installer is `scripts/install.sh`. It will be added with the CLI bootstrap implementation once the `docker-host` executable can install and manage the Host container lifecycle.
